export enum FlexValueEnum {
  ONE = 1,
}

export enum StorageKeyEnum {
  AUTH_DATA = '@AuthData',
}

export enum PerPageEnum {
  DEFAULT = 10,
}

export enum QueryKeyEnum {
  MOVIES_LIST = '@MoviesList',
  FAVORITES_MOVIES_LIST = '@FavoriteMoviesList',
}
